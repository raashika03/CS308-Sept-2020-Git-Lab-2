/*Main Program which accepts two vectors, and
prints their sum, elementwise product, difference of the two vectors, dot product of
the two vectors, and the angle between the two vectors. */
#include <stdio.h>
#include "veclib.h"
int int main(int argc, char const *argv[])
{
	/* code */
	vector vecA, vecB, vecC;//declaring the two vectors
	//read vectors vecA, vecB as input
	printf("printing sum of two vectors\n", );
	//add(vecA, vecB, vecC);
	/*
	Similarly,
	print other outputs like elementwise product
	difference of the two vectors
	dot product of the two vectors
	angle between the two vectors
	*/
	return 0;
}